import React from 'react';
import { View } from 'react-native';
import Login from '../login/Login';

const Main: React.FC = () => {
  return (
    <View>
      <Login />
    </View>
  );
};

export default Main;
